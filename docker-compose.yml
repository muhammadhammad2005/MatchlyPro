version: '3.8'

services:
  resumematcherai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: resumematcherai-resume-matcher
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - PORT=8080
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - resumematcherai-network
    volumes:
      - ./logs:/var/log/nginx
    labels:
      - "com.resumematcherai.description=Resume Matcher Application"
      - "com.resumematcherai.maintainer=ResumeMatcherAI Team"

  # Optional: Add a database service if needed in the future
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: resumematcherai-db
  #   environment:
  #     POSTGRES_DB: resumematcherai
  #     POSTGRES_USER: resumematcherai_user
  #     POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   networks:
  #     - resumematcherai-network
  #   restart: unless-stopped

  # Optional: Add Redis for caching if needed
  # redis:
  #   image: redis:7-alpine
  #   container_name: resumematcherai-redis
  #   ports:
  #     - "6379:6379"
  #   networks:
  #     - resumematcherai-network
  #   restart: unless-stopped

networks:
  resumematcherai-network:
    driver: bridge
    name: resumematcherai-network

# volumes:
#   postgres_data:
#     driver: local