version: '3.8'

services:
  matchly:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: matchly-resume-matcher
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - PORT=8080
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - matchly-network
    volumes:
      - ./logs:/var/log/nginx
    labels:
      - "com.matchly.description=Resume Matcher Application"
      - "com.matchly.maintainer=Matchly Team"

  # Optional: Add a database service if needed in the future
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: matchly-db
  #   environment:
  #     POSTGRES_DB: matchly
  #     POSTGRES_USER: matchly_user
  #     POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   networks:
  #     - matchly-network
  #   restart: unless-stopped

  # Optional: Add Redis for caching if needed
  # redis:
  #   image: redis:7-alpine
  #   container_name: matchly-redis
  #   ports:
  #     - "6379:6379"
  #   networks:
  #     - matchly-network
  #   restart: unless-stopped

networks:
  matchly-network:
    driver: bridge
    name: matchly-network

# volumes:
#   postgres_data:
#     driver: local